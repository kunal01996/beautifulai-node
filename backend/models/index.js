/**
 * After adding all your models in the models folder, do remember to add them over here, otherwise they would
 * not be loaded by the system.
 */

exports.models = {

    // add your models
    
}
